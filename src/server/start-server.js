
// This script is used to start the database server
import app from './index.js';

// Ensure the server instance is exported and started correctly
console.log('Database server started on port 5000');
